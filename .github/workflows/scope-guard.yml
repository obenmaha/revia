name: Scope Guard

on:
  pull_request:
    branches: [ main, develop, feat/*, feature/* ]
    paths:
      - 'src/**'
      - 'scripts/scope-guard.mjs'
      - '.github/workflows/scope-guard.yml'

jobs:
  scope-check:
    name: Vérification de la portée du code
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Configuration de Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Installation des dépendances
        run: npm ci
      
      - name: Exécution du scope guard
        run: |
          chmod +x scripts/scope-guard.mjs
          node scripts/scope-guard.mjs
      
      - name: Commentaire sur la PR (en cas d'échec)
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            
            const botComment = comments.find(comment => 
              comment.user.type === 'Bot' && 
              comment.body.includes('🚫 Scope Guard - Violations détectées')
            );
            
            if (!botComment) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: `## 🚫 Scope Guard - Violations détectées
                
                Le scope guard a détecté des termes interdits dans le code source. 
                
                **Termes interdits:** \`cabinet\`, \`patient\`, \`rdv\`, \`facturation\`
                
                Veuillez supprimer ou remplacer ces termes avant de pouvoir merger cette PR.
                
                Consultez les logs de l'action pour plus de détails.`
              });
            }
